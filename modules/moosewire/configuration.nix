{...}: {
  # moosewire is a per-user TUI installed via home-manager (see ./home.nix);
  # there is no system-level component. This stub exists so the module resolves
  # for both the system and home import passes (see CLAUDE.md's module rule).
}
