# tci-server is a system-level fleet service only; no home-manager component.
# Stub kept so the module resolves for both the system and home import passes
# (see CLAUDE.md's "every module ships both files" rule).
{...}: {}
